import React from "react";
import { RoleDashboard } from "../components/dashboard/RoleDashboard";
import { OrgDashboardScreen } from "../screens/organization/OrgDashboardScreen";

/**
 * 🔵 Teammate B — Organization navigator. Own this file.
 * Showcases the demo dashboard for now; swap in your screens (OrgDashboardScreen, etc.)
 * as you build them.
 */
export const OrgNavigator: React.FC = () => {
  // TODO(B): replace this return with your routing once screens are ready.
  return <RoleDashboard />;
};

export default OrgNavigator;